# AutoRNA main Snakefile (QC-only workflow)

# Load config from project root
configfile: "../config.yaml"

SAMPLES = config["samples"]

rule all:
    input:
        expand("data/qc/{sample}_fastp.json", sample=SAMPLES)

include: "rules/qc.smk"
